//console.writeline("olá, tudo bem?");

//console.write("poderia digitar o seu primeiro nome?");

//string nome = console.readline();

//console.write("poderia digitar o seu sobrenome?");

//string sobrenome = console.readline();

//console.writeline($"olá {nome} {sobrenome}, seja bem-vindo(a) ao bethany pie shop hrm!");

//tipos de variaveis


// int = tipo da variavel / numerointeiro = nome da variavel / 10 = valor atribuido a variavel
//int numerointeiro = 10;

//criando varias variaveis do mesmo tipo na mesma linha
//int months = 12, bonus = 1000;


//boolean = tipo da variavel / verdadeirooufalso = nome da variavel / true = valor atribuido a variavel (apenas aceita true ou false)
//bool verdadeirooufalso = true;

//double = tipo da variavel / numeroqueaceitavirgula = nome da variavel / 3.14 = valor atribuido a variavel (doublu aceita número flutuantes = que possuem casas decimais = números com virgula)

//double numeroqueaceitavirgula = 3.14;


//byte = tipo da variavel / funcionarios = nome da variavel / 255 = valor atribuido a variavel (byte aceita números inteiros de 0 a 255)
//byte funcionarios = 255;




//podemos utilizar o const para impossibilitar a alteração do valor da variavel exemplo:
//const double pi = 3.14;
//pi = 3.15; // isso gerará um erro pois a variavel foi definida como const



//const float primeiroValor = 1.75f;
//float segundoValor;

//segundoValor = 2.50f;

//float resultado;

//resultado = primeiroValor + segundoValor;

//Console.WriteLine($"A soma do primeiro valor ({primeiroValor}) com o segundo valor ({segundoValor}) é igual a: {resultado} ");

// verificando os valores máximos e mínimos que cada tipo de variável pode armazenar
//int intMaxValue = int.MaxValue;
//int intMinValue = int.MinValue;

//char userSelection = 'a';

//char versaoMaiscula = char.ToUpper(userSelection);

//bool eUmDigito = char.IsDigit(userSelection);

//bool eUmaLetra = char.IsLetter(userSelection);


//Dia

//DateTime umDiaQualquer = new DateTime(2025, 09 , 13);
//Console.WriteLine(umDiaQualquer);


//DateTime dataComeco = umDiaQualquer.AddDays(30); // um mês dps da data umDiaqualquer

//DateTime diaDeHj = DateTime.Now; // capiturando o ida e a hora atual

//bool horarioverao = diaDeHj.IsDaylightSavingTime(); // verificando se a data atual está em horário de verão

//Console.ReadLine();
